# Keep binary assets byte-exact regardless of a contributor's core.autocrlf
# setting — a stray CRLF filter silently corrupts a PDF or SQLite file.
*.pdf      binary
*.db       binary
*.sqbpro   binary
